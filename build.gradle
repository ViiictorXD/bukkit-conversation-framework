plugins {
    id "java"
    id "com.github.johnrengelman.shadow" version "8.1.1"
}

group = "io.github.viiictorxd"
version = "1.0.0-SNAPSHOT"

repositories {
    mavenCentral()

    maven { url = "https://maven.elmakers.com/repository/" } /* Spigot */
}

dependencies {
    compileOnly(libs.getSpigot())

    compileOnly(libs.getLombok())
    annotationProcessor(libs.getLombok())
}

tasks {
    shadowJar {
        archiveFileName.set("BukkitConversationFramework.jar")
        destinationDirectory.set(file("G:/Meu Drive/Minecraft/Servers/1.8.8 Paper/plugins"))
    }
}

java {
    sourceCompatibility = "8"
    targetCompatibility = "8"
}